

const Filter = ({placeholder, value, onChange}) => {
  return (
    <div>Filter shown with <input placeholder={placeholder} value={value} onChange={onChange}/> </div>
  )
}

export default Filter